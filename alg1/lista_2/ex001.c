#include <stdio.h>

nodo *novaLista(nodo *lista){
	nodo *novo = (nodo *)malloc(sizeof(novo));
	novo -> info = NULL

}

typedef struct Nodo {
	int info;
	struct Nodo *prox;
} nodo;

int main(){

	nodo *lista;

	return 0;
}

